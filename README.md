
  # Couples Conversation App UI

  This is a code bundle for Couples Conversation App UI. The original project is available at https://www.figma.com/design/3QEbPsO247QuUfM0URAxF7/Couples-Conversation-App-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  